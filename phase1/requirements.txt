requests>=2.31.0
httpx>=0.27.0
pandas>=2.2.0
pydantic>=2.6.0
python-dotenv>=1.0.0
pytest>=8.0.0

# Phase 3: display-only LLM summaries (optional — only imported if OPENAI_API_KEY is set)
openai>=1.30.0

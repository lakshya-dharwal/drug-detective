# Phase 2 API layer
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0
supabase>=2.4.0

# Phase 1 pipeline runtime deps (imported as a module; kept in sync with ../../phase1/requirements.txt)
requests>=2.31.0
httpx>=0.27.0
pandas>=2.2.0
pydantic>=2.6.0

# Phase 3: display-only LLM summaries (optional at runtime — only used if OPENAI_API_KEY is set)
openai>=1.30.0

# Production build config (public, non-secret). Points the UI at the deployed
# Fly.io backend. NEXT_PUBLIC_* vars are inlined into the client bundle at build.
NEXT_PUBLIC_API_BASE_URL=https://drug-detective-api.fly.dev
